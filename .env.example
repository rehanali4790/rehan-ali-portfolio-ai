# Copy to `.env` for local development, or set the same variables in Vercel
# (Project → Settings → Environment Variables).

# Prisma (required for `next build` / Prisma Client). Path is relative to `prisma/`.
DATABASE_URL="file:./dev.db"
